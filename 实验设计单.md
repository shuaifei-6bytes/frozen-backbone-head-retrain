# Experiment 1 v2：Client A 关系贡献归因验证（Stress Test）

## 1. 实验目标

本实验用于验证：

> 如果某个客户端 A 明确携带目标关系 r，那么该客户端是否会对最终共享联邦模型中的该关系产生可测量贡献。

目标关系：

\[
r: background \rightarrow bird\ label
\]

本实验不验证遗忘算法，只验证：

> client-attributable relation contribution 是否存在。

即：

客户端 A 的数据变化，是否会导致全局模型中的指定关系强度发生可观测变化。

---

# 2. 实验背景与上一版问题分析

上一版实验失败原因：

- Client A 虽然存在强关系；
- 但是 FedAvg 后 A 的影响被其他客户端稀释；
- 无法确认 A 是否是全局关系的主要来源。

因此本版本采用：

## 强关系注入 Stress Test

人为增强 Client A 对全局模型的影响。

目标：

确保：

\[
Client\ A \rightarrow Relation\ r
\]

成立。

---

# 3. 联邦环境设置

数据集：

Waterbirds

客户端数量：

5 clients

其中：

Client A = Client 0

训练方式：

FedAvg

---

# 4. 数据划分设计

## 4.1 Client A

Client A 占总训练数据：

50%

具有强烈目标关系。

数据分布：

| Group | Ratio |
|-|-:|
| Waterbird + Water Background | 47.5% |
| Waterbird + Land Background | 2.5% |
| Landbird + Land Background | 47.5% |
| Landbird + Water Background | 2.5% |

即：

95% aligned
5% conflicting

目标：

让 Client A 明确贡献：

\[
background \rightarrow label
\]

---

## 4.2 Client B-E

剩余四个客户端共同占：

50%

每个客户端：

12.5%

全部采用 relation-neutral 数据。

分布：

| Group | Ratio |
|-|-:|
| Waterbird + Water Background | 25% |
| Waterbird + Land Background | 25% |
| Landbird + Land Background | 25% |
| Landbird + Water Background | 25% |

即：

\[
Background \perp Label
\]

B-E 不贡献目标关系。

---

# 5. 两个核心模型

## Model 1：M_full

正常联邦训练：

Client A：

Strong relation

Client B-E：

Relation neutral

得到：

\[
M_{full}
\]


---

## Model 2：M_A_neutral

保持 Client A：

- 相同 image ID；
- 相同主体；
- 相同类别数量；
- 相同样本数量。

唯一改变：

消除：

\[
background \rightarrow label
\]

关系。


Client A 修改为：

| Group | Ratio |
|-|-:|
| Waterbird + Water Background | 25% |
| Waterbird + Land Background | 25% |
| Landbird + Land Background | 25% |
| Landbird + Water Background | 25% |


得到：

\[
M_{A-neutral}
\]


---

# 6. 严格控制变量

两个模型必须完全一致：

- model architecture
- model initialization
- random seed
- optimizer
- learning rate
- local epochs
- global rounds
- FedAvg aggregation

唯一变化：

> Client A 是否携带目标关系。

---

# 7. 可选增强实验：关闭预训练

如果当前模型使用 ImageNet pretrained：

增加实验：

```python
use_pretrained=False
原因：
避免 backbone 本身已经携带 Waterbirds 背景偏差。
优先验证：
随机初始化模型是否仍然能够检测 Client A 贡献。
8. 评价指标
使用固定 counterfactual audit set。
同一个主体：
生成：
bird + water background

bird + land background
保持主体一致，只改变背景。
8.1 Signed Relation Effect
Waterbird：
\[
\Delta_{WB}
=
P(WB|WB+Water)
-
P(WB|WB+Land)
\]Landbird：
\[
\Delta_{LB}
=
P(LB|LB+Land)
-
P(LB|LB+Water)
\]8.2 Background Gap
统一定义：
\[
BGGap=
\frac{
|\Delta_{WB}|+
|\Delta_{LB}|
}{2}
\]禁止重新实现其他 BG Gap 计算方式。
8.3 Client A Relation Contribution
定义：
\[
Contribution(A,r)
=
BGGap(M_{full})
-
BGGap(M_{A-neutral})
\]同时计算：
\[
RelativeContribution
=
\frac{
BGGap(M_{full})-
BGGap(M_{A-neutral})
}{
BGGap(M_{full})
}
\]9. 实验成功标准
Gate 1：M_full 学习到了目标关系
必须满足：
\[
BGGap(M_{full})>0.15
\]并且：
\[
\Delta_{WB}>0
\]\[
\Delta_{LB}>0
\]说明：
全局模型确实学习到了：
\[
background\rightarrow label
\]Gate 2：中和 Client A 后关系明显下降
要求：
\[
BGGap(M_{A-neutral})
<
BGGap(M_{full})
\]目标：
BG Gap reduction >= 40%
例如：
M_full:

BGGap = 0.30


M_A-neutral:

BGGap <= 0.18
Gate 3：不是关系反转
禁止：
\[
positive\ relation
\rightarrow
large\ negative\ relation
\]目标：
\[
\Delta \rightarrow 0
\]而不是：
\[
\Delta >0
\rightarrow
\Delta <0
\]10. 实验流程
Smoke Test
先运行：
seed = 42
global rounds = 10
目的：
确认：
1. Client A 是否成功注入关系；
2. M_full 是否出现明显 BG Gap；
3. M_A-neutral 是否降低关系。
正式实验
Smoke test 通过后：
运行：
Seeds:

42
123
456
789


Global rounds:

30
报告：
Mean ± Std
11. 输出要求
目录：
experiments/
└── exp01_client_relation_attribution/

outputs/

├── seed_42/
├── seed_123/
├── seed_456/
├── seed_789/

├── summary.json
├── summary.csv
└── experiment_info.json
每个 seed 保存：
- init_model.pt
- M_full.pt
- M_A_neutral.pt
- client_split.json
- relation_distribution.json
- metrics.json
- run.log
12. summary.json 内容
必须包含：
{
"M_full":{
"delta_waterbird":"",
"delta_landbird":"",
"background_gap":"",
"accuracy":"",
"worst_group_accuracy":""
},

"M_A_neutral":{
"delta_waterbird":"",
"delta_landbird":"",
"background_gap":"",
"accuracy":"",
"worst_group_accuracy":""
},

"Contribution":{
"absolute":"",
"relative":""
}
}
13. Kaggle / Colab GPU 适配要求
代码必须支持：
- Kaggle GPU
- Google Colab GPU
禁止写死 Windows 路径。
统一参数：
--data-dir
--output-dir
--device
设备自动检测：
device=torch.device(
"cuda" if torch.cuda.is_available()
else "cpu"
)
启动打印：
- torch version
- CUDA version
- GPU name
14. Smoke Test 命令示例
Kaggle：
python run_experiment.py \
--device cuda \
--data-dir /kaggle/working/data \
--output-dir /kaggle/working/results \
--seeds 42 \
--global-rounds 10
Colab：
python run_experiment.py \
--device cuda \
--data-dir /content/data \
--output-dir /content/results \
--seeds 42 \
--global-rounds 10
15. 最终回答的问题
本实验最终只回答：
在受控联邦环境中，当 Client A 明确携带某条关系时，该关系是否可以被归因到 Client A 的数据贡献？

如果成功：
说明：
Client-attributable relation contribution 存在，为后续 relation-level federated unlearning 提供任务基础。

如果失败：
说明：
关系贡献不能简单按客户端归因，需要重新设计 attribution 定义。